const jatin = function (a, b) {
    return a = b;
};

console.log(jatin(2, 3));

// A function is like a recipe. You give it some
// ingredients (called parameters), and it follows
//  a set of instructions to give you a result
// (called return value)