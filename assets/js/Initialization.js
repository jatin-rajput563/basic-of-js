// Initialization in JavaScript refers to the process of assigning
// a value to a declared variable. A variable can be declared without
// being initialized, but it will be undefined until it is assigned a value.

// -------------without-Initialization----------------//
let freeFire;
console.log(freeFire);
// -------------with-Initialization-------------------//
let y = "rajput";
console.log(y);
